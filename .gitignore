# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Local runtime state: the workbook volume and its snapshots live outside the repo.
# The DELIVERABLE workbook at ZacoAgents_Chris/workbook/account-sales-book.xlsx is committed.
.local/
*.xlsx.bak
~$*.xlsx

# Secrets and environment
.env
.env.*
!.env.example

# Editor / OS
.idea/
.DS_Store
Thumbs.db

# Frontend
node_modules/
# The SPA is a build artefact, produced by `npm run build` and by the Docker image. It is not
# committed: the sources under ZacoAgents_Chris/frontend/src are the record of it, and a stale
# committed bundle is worse than none -- it serves silently.
ZacoAgents_Chris/zaco/web/spa/
frontend/dist/
*.tsbuildinfo
